# Self-QA — CTA Section (3.6)

Custom or templated? Custom. The clip-path left→right curtain reveal on the ADU interior image is dramatic and premium. The copper stat tag sliding in from +40px with a separate 550ms delay creates a two-beat reveal (image first, stat tag second) that signals deliberate choreography.

Continuous motion? Yes. The left column stagger (label → headline lines → body → CTAs → trust items) runs in sequence. The right column clip-path reveal fires 150ms after the trigger to prevent simultaneous activation.

Proportions editorial? Strong. Headline at clamp(3rem, 6vw, 5.5rem) commands the section. The copper fill-sweep on the primary CTA button (translateX -101% → 0) is premium interaction design. The ADU interior image at 4/5 ratio is tall and commanding.

Pass as $40k build? Yes. Pass.
