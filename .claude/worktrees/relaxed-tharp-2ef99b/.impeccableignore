# 828 Construction — Impeccable suppression rules
# Run `npx impeccable detect components/ app/` to check

# pure-black-white: intentional brand decision.
# The 828 design system is black/white/gray/copper only.
# bg-black (#000) is used for editorial dark sections.
# Cleanup target (non-urgent): standardize to bg-[#0a0a0a] for consistency.
[pure-black-white]

# Email template at app/api/contact/route.ts uses Arial and #000 correctly.
# HTML email clients don't support web fonts; Arial is the correct choice.
app/api/contact/route.ts
